<h1><%= current_user.email%>'s' Cart</h1>

<% @cart.each do | cart_item | %>
  <h3><%  cart_item.toy.product %>: <% cart_item.quantity_in_stock %> * <% number_to_currency(cart_items.toy.cost) %> </h3>
  <h3><% link_to 'Remove', "/carted_products#{carted_item.id}" method: :delete %></h3>
  <% end %>

  <% link_to 'Buy Now!', '/orders', method: :post, class: "btn btn-default" %>

  <p> <%= @test_token%> </p>
  <p> <%= @test_secret%> </p>